export interface DataEntry {
    id: number
    name: string
    duration: number
    enterTime: number
}

export interface NewDataEntry {
    name: string
    duration: number
    enterTime: number
}
