export interface DataEntry {
    id: string
    name: string
    duration: number
    enterTime: number
}

export interface NewDataEntry {
    name: string
    duration: number
    enterTime: number
}
